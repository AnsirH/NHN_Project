# AI 활용 기술 문서 — Clay Wars (가제)

> NHN NAN 2026 게임잼 제출물. 개발 과정에서 우리 팀이 AI를 어떻게 활용했는지 —
> 사용한 도구, 워크플로, 주요 프롬프트, 산출물 검증 방법 — 를 정리한 문서입니다.
>
> 본 문서의 수치와 사례는 저장소에 남긴 실제 기록에 근거합니다 —
> 세션 단위 상세 로그: `Assets/Docs/ai_usage_log.md` / 개발 이력: git 커밋 로그.

---

## 1. 개요 — 사용한 AI 도구와 활용 규모

| 도구 | 용도 | 적용 범위 |
|---|---|---|
| **Claude Code** (Anthropic) | 기획 문서 관리 · 시스템 설계 · 코드 구현 · 테스트 작성 · 디버깅 · 밸런싱 도구 구축과 운용 · 문서화 | 개발 전 과정 |
| **VARCO** | 병사 3D 캐릭터 모델 제작 | 병사 모델 5종 (기본·전사·궁수·사냥꾼·암살자) |
| **ChatGPT · Gemini** | 게임 내 2D 이미지 제작 | (§7 참조) |
| **MCP for Unity** | Claude Code와 Unity 에디터를 연결하는 자동화 계층 | 컴파일 확인 · 테스트 실행 · 씬/에셋 작업 |

**활용 규모** (2026-07-12 개발 시작, 약 4주):

- 코드는 Claude Code로 개발했습니다. AI 세션에서 나온 커밋에는 공동 작성(`Co-Authored-By`) 트레일러를 남기는 규칙을 두어, 전체 커밋 151건 중 64건이 어떤 AI 세션의 산출물인지 git 이력으로 확인할 수 있게 했습니다.
- Unity EditMode 테스트 305개 전체 통과 상태를 유지하며 개발했습니다 (인게임 + 아웃게임 통합 기준).
- 밸런싱 파이프라인을 구축해 전투 500판 시뮬레이션을 3.1초에 실행하고, 시나리오 매트릭스 14종 × 각 200판을 명령 한 번으로 검증했습니다.
- 에디터↔CLI 교차 검증(시드 100개 × 시나리오 2종 = 200판)에서 승자 뒤집힘 0건을 확인했습니다.

## 2. 팀 구성과 역할 분담

<!-- [작성 예정]
- 프로그래머 2인: 인게임(전투) 담당 / 아웃게임 담당 구분
- 파트별로 AI를 활용한 범위 요약 (상세는 §4~§7)
-->

- _(작성 예정)_

## 3. AI 중심 개발 워크플로 — 인게임(전투) 파트, 구조 설명

우리는 "AI에게 코드를 시킨다"에 그치지 않고, **AI가 산출물을 스스로 검증하고 판단 근거를 만들 수 있는 구조를 먼저 설계**하는 데 시간을 들였습니다. 네 개의 축으로 구성됩니다.

### 3.1 아키텍처가 곧 AI 활용 전략 — 로직/뷰 분리와 결정론

- 전투 시뮬레이션(`Simulation` 어셈블리)은 Unity 엔진 참조가 없는 순수 C#으로 작성했습니다 (`noEngineReferences`, `System.Numerics` 수학, 고정 틱 + 시드 결정론).
- 이렇게 분리한 목적 자체가 AI 활용입니다. Unity 에디터를 띄우지 않고 헤드리스로 전투를 대량 실행해 승률 통계를 얻을 수 있고, 같은 시드는 항상 같은 결과를 내므로 "수치 하나를 바꾸면 결과가 어떻게 달라지는가"를 재현 가능한 실험으로 확인하며 작업할 수 있습니다.
  - 실측 예: 밸런스 수치 1개(장군 패시브 0.15→0)만 바꾸자 기준 전투가 339틱→372틱으로 변화, 원복하면 정확히 339틱이 재현됐습니다 — "도구가 실제 게임 데이터를 읽고 있다"는 증명으로 사용했습니다.
- 이를 기반으로 Unity 밖에 dotnet CLI 밸런싱 러너(`Tools/BalanceLab`)를 구축했습니다. 시뮬레이션 소스를 링크 컴파일(복사 0)하고, 밸런스 수치는 Unity `.asset` 파일을 직접 파싱(fail-fast 미니 YAML 파서)해 수치의 단일 출처를 보장합니다.

### 3.2 데이터 드리븐 — 코드 수정 없이 콘텐츠를 추가하는 구조

- 병과·장군·스킬은 전부 ScriptableObject 데이터로 정의했습니다. **"장군 1명 추가 = 데이터 에셋 1개"**가 설계 원칙이고, 이 원칙이 깨지면 실패하는 테스트(`GeneralDataAsset_DrivesSimulation_WithoutCodeChanges`)를 두어 구조를 강제했습니다.
- 덕분에 새 콘텐츠 작업은 코드가 아니라 데이터 생성이 되어 변경 범위가 좁고, 위 테스트가 아키텍처 붕괴를 자동으로 감지합니다.

### 3.3 승인 게이트와 기록 원칙 — AI의 재량을 구조로 통제

한 세션의 표준 진행 순서를 다음과 같이 고정했습니다.

```
① 확정 변경점 제시 (기획 결정)
② AI가 기획 문서 갱신 + 기존 서술과의 모순 일괄 수정   ← 파트 A (문서)
③ 검토·승인
④ AI가 코드 구현 + 테스트 작성                        ← 파트 B (코드)
⑤ MCP for Unity로 컴파일 에러 0 확인
⑥ EditMode 테스트 전체 통과 확인
⑦ BalanceLab 회귀 확인 (기준 시드의 전투 결과 보존 여부)
⑧ 세션 로그 기록 (ai_usage_log.md — 도구/목적/프롬프트 요지/산출물/검증)
```

- 기획 문서(`nan2026_design_doc.md`)를 단일 참조 문서로 유지하고 **"문서에 없는 기능은 프로토타입에 넣지 않는다"**를 명문화해, AI가 임의로 작업 범위를 넓히지 못하게 했습니다.
- 프롬프트·활용 내역은 나중에 몰아서 정리하지 않고 세션 종료 시 즉시 기록하는 것을 기획 문서의 요구사항으로 명시했습니다.
- 커밋 메시지는 한국어 카테고리 프리픽스(`시뮬:`, `뷰:`, `씬:`, `에셋:` 등)로 통일하고, AI 세션 산출물에는 공동 작성 트레일러를 남겼습니다.

### 3.4 MCP for Unity — 에디터 확인 작업의 자동화

- Claude Code를 MCP(Model Context Protocol)로 Unity 에디터에 연결해, 컴파일 에러 확인·EditMode 테스트 실행·씬 오브젝트 배치·머티리얼/프리팹 작업을 세션 안에서 처리하게 했습니다.
- 이 연결이 없으면 "AI가 코드 작성 → 사람이 에디터로 가서 컴파일·테스트 확인 → 결과를 다시 AI에게 전달"하는 왕복이 매번 발생합니다. 연결 후에는 작성—컴파일 확인—테스트 실행—결과 보고가 한 세션 안에서 끝나, 사람은 승인과 판단에만 집중할 수 있었습니다.
- 외부 에셋 도입 시 URP 머티리얼 변환(분홍 텍스처 수정), 병과별 사운드 연결, 전장 장식 배치 같은 에디터 작업도 같은 경로로 진행했습니다.

## 4. 주요 프롬프트 및 지시 사항 — 인게임 파트 대표 사례

세션 단위 로그(`Assets/Docs/ai_usage_log.md`)에서 발췌한 대표 사례입니다. AI를 단순 코드 생성기가 아니라 **리뷰어·조사자·판단 보조자**로 활용한 사례를 중심으로 골랐습니다.

### 사례 1 — 기획 개정의 일괄 반영: 문서와 코드를 하나의 지시로 동기화

- **프롬프트 요지**: "기획 v4 확정 변경점 3개 — ① 병사 트리거 기믹 전면 삭제(개성은 이동 패턴+타겟팅+스탯만) ② 장군 액티브를 '전투당 1회'에서 조건 충전식으로 전환 + 4종 확정 ③ 플레이어 스킬 4종 확정 — 을 반영하라. 먼저 기획 문서를 갱신하며 모순되는 서술을 일괄 수정하고, 승인 후 코드에 반영하라."
- **산출물**: 기획 문서 14개 섹션 일관성 수정 → 시뮬레이션 계층(enum 어휘 확장, 장군 정의 신설, 충전 시스템, 부대 스코프 스킬 4종, 긍정 상태이상), 데이터 에셋 6개 생성, 뷰 계층(장군 하이라이트, 버프 틴트, HUD 버튼 자동 복제), 신규 테스트 5개.
- **검증**: EditMode 14/14 통과 + 헤드리스 실측 — 장군 유/무 30판에서 장군 측 30승, 충전식 액티브 4종이 "전투당 2~3회" 설계 의도대로 발동하는지 10판 평균으로 확인(방진 2.1회, 일제 사격 1.9회 등).
- **부수 효과**: 실측 중 "분대 선두에 서는 장군이 집중 포화를 받아 HP가 낮으면 액티브 1회 발동 전에 죽는다"는 관찰이 나와, 기획의 "롤별 장군 스탯 차등" 조항에 대한 실측 근거를 얻었습니다.

### 사례 2 — 도구 구축 전 스펙 리뷰: AI의 비판적 검토를 먼저 받는다

- **프롬프트 요지**: "Unity 밖에서 시뮬레이션을 대량 실행하는 CLI 러너를 만들어라. 밸런스 수치의 단일 출처는 Unity .asset 파일이다(복사 금지, 직접 읽어라). **구현 전에 스펙을 리뷰하라.**"
- **경과**: AI가 구현에 들어가기 전 스펙에서 결함 3건·리스크 2건을 지적했고, 이를 반영한 v2 스펙으로 구현을 진행했습니다.
- **산출물**: 시뮬 소스 링크 컴파일 + .asset 직독 파서 + 시나리오 JSON 체계. 파서는 의도적으로 fail-fast(모르는 형식이면 조용히 넘기지 않고 즉시 실패)로 설계했는데, 이후 에디터가 에셋을 재저장하며 enum 리스트를 16진 바이트 문자열로 직렬화하는 예외 형식이 나타났을 때 조용한 오해석 대신 즉시 오류로 드러나 파서를 보완할 수 있었습니다.

### 사례 3 — 이상 감지와 작업 중단 판단: "튜닝 전에 원인 규명이 먼저"

- **상황**: 목표 승률 밴드(장군 유/무 65~75%, 미러 매치 45~55% 등)를 데이터로 정의하고 첫 전체 매트릭스(14종 × 200판)를 실행 — 결과 1 PASS / 13 FAIL.
- **AI의 보고**: 13건의 FAIL 중 **미러 매치 실패는 성격이 다르다**는 분리 보고를 받았습니다. 완전히 동일한 구성끼리 붙였는데 좌/우 승률이 암살자 16.5%, 사냥꾼 40.0%, 전사 57.5%로 편향 — 이는 데이터가 아니라 시뮬 구조(유닛 처리 순서 등)의 문제이며, "이대로 튜닝에 들어가면 모든 밸런스 수치가 오염되므로 수치 튜닝 이전에 원인 규명이 필요하다"는 작업 순서 변경 제안이었습니다.
- **의미**: 지시한 작업(밴드 판정 도구 완성)을 끝내는 데 그치지 않고, 결과 데이터의 이상 신호를 읽어 프로젝트 리스크를 먼저 알린 사례입니다.

### 사례 4 — 심층 디버깅: 증상 → 소거 실험 → 원인 판단 → 계약 재정의

- **상황**: 같은 시드의 전투를 Unity 에디터(Mono)와 dotnet CLI(CoreCLR)에서 돌리면, 수백 틱 동안 비트 단위로 일치하다가 드문 판에서만 1~2틱 차이가 발생했습니다.
- **조사 과정** (소거 실험):
  1. 삼각함수가 없는 시나리오에서도 재현됨을 확인 → 초월함수 단독 원인 배제.
  2. `DOTNET_EnableFMA=0`으로 재실행 → 변화 없음 → FMA 배제.
  3. `DOTNET_EnableHWIntrinsic=0`으로 재실행(실행이 3배 느려진 것으로 플래그 적용을 확인) → 변화 없음 → 하드웨어 인트린식 전체 배제.
  4. 남는 가설로 "Mono JIT는 float 중간값을 확장 정밀도로 유지(ECMA CIL 허용)하고 CoreCLR은 연산별로 엄격 반올림한다"는 런타임 차이를 지목 — 반올림 경계에 걸린 드문 비교 하나가 뒤집히는 패턴과 일치하며, C# 코드 수정으로는 신뢰성 있게 제거할 수 없다는 판단을 받았습니다.
- **결정 (계약 재정의)**: 비트 단위 결정론 요구를 폐기하고 정본을 분리(밸런싱 통계=CLI, 리플레이=에디터)한 뒤, 교차 검증을 "승자 불일치 무관용 + 시드 100개" 통계 검증으로 재정의해 승인했습니다.
- **후속 정밀화**: 1차 허용치(절대 ±5틱)로 100시드를 돌리자 승자는 0/200건 뒤집힘 없이 유지되는데 틱 차이만 초과하는 시드가 나왔고, 분포 분석으로 이 드리프트가 고정 오차가 아니라 나비효과(갈라진 뒤 전투 길이에 비례해 커짐)임을 확인 → 허용치를 상대 오차 ±20%로 재정의했습니다. 세션 로그에 남긴 교훈: *"허용 오차는 오차의 생성 메커니즘에 맞춰 정의해야 한다."*

### 사례 5 — 회귀 방어를 설계에 내장: 기능을 추가해도 기존 결과가 보존되게

- **상황**: 아웃게임 파트와의 합의로 방어력(감쇠 공식)·치명타(확률) 스탯을 시뮬에 추가해야 했습니다. 결정론 시뮬에서는 난수 소비가 하나만 늘어도 이후 모든 전투 결과가 달라집니다.
- **설계**: **"치명타 확률이 0이면 난수를 뽑지 않는다"** — 기존 에셋은 신규 스탯이 전부 0이므로 감쇠 배율 1 + 난수 소비 0이 되어, 기능 추가 전후 기존 전투 결과가 비트 단위로 보존됩니다.
- **검증**: 기준 시드 전투가 확장 전후 동일(339틱), 500판 승률 동일. 도트 피해에는 방어력을 적용하지 않는 기획 의도(상태이상 셋업의 가치 보존)를 피해 누적기 분리로 구현하고, 이를 강제하는 테스트를 함께 작성했습니다.
- 같은 원칙의 연장으로, 아웃게임이 전달하는 스탯은 불변 정의를 수정하지 않고 경계에서 새 인스턴스로 결합(`WithStats` 팩토리 패턴)해, 같은 장군 에셋이 레벨이 다른 여러 분대에 동시에 쓰여도 간섭이 없게 했습니다.

### 사례 6 — 협업 계약 추종: 세 차례 바뀐 계약을 흡수하고, 만든 것을 버리는 판단까지

- **상황**: 아웃게임↔인게임 간 전투 데이터 계약이 개발 중 세 차례 변경됐습니다 (스탯 미전달 → 인게임 재계산 → 최종 스탯 직접 전달).
- **활용**: 팀원의 브랜치를 머지 없이 git으로 조사해 계약 코드를 분석시키고, 변경마다 인게임 쪽 수신 구조를 재작성했습니다. 특히 2차 변경 때는 하루 전에 구축한 CSV 스탯 파이프라인에 대해 **"스탯 정본이 아웃게임으로 확정된 이상 우리 CSV는 중복 출처가 되어 '튜닝했는데 게임엔 반영 안 되는' 함정이 된다"는 폐기 제안**을 AI로부터 받았고, 이를 승인해 파이프라인을 걷어냈습니다 — 만든 것을 유지하는 쪽이 아니라 데이터 정합성을 우선한 결정이었습니다.
- **해석 위임 사례**: 아웃게임이 "스킬 강화 증강을 몇 번 골랐는지"(횟수)만 넘기고 효과 해석을 인게임에 위임하자, 기획의 계층 구조("장군 액티브는 '사건' 계층 — 체감을 지배하는 것은 발동 빈도")를 근거로 충전 필요량 감소로 번역하고, 강화가 쌓여도 스킬이 소음이 되지 않도록 하한을 둔 공식(`max(기본 × (1 − 0.15 × 횟수), 기본 × 0.4)`)을 설계했습니다. 실측: 강화 0회 1발동 → 4회 2발동.
- **머지 검증**: 두 파트 통합 머지에서 충돌은 설정 파일 3건뿐(코드·에셋 충돌 0), 통합 후 EditMode 299/299 통과와 씬 플레이 정상을 확인했습니다.

### 프롬프트 작성 원칙 (전 세션 공통)

1. **확정된 결정만 전달한다** — 미확정 사항은 기획 문서의 미확정 섹션에 명시하고 구현을 금지.
2. **문서 먼저, 코드는 승인 후** — 파트 A(문서) / 파트 B(코드) 분리 진행.
3. **검증 수단을 함께 요구한다** — 구현 지시에는 테스트·실측 조건을 포함.
4. **큰 작업은 스펙 리뷰를 선행한다** — "구현 전에 스펙을 리뷰하라"로 비판적 검토를 먼저 받음.
5. **세션 즉시 기록** — 도구/목적/프롬프트 요지/산출물/검증을 세션 종료 시 로그에 남김.

## 5. 아웃게임 파트 AI 활용

<!-- [담당 팀원 작성 예정]
권장 포함 내용:
- 사용한 AI 도구 (Claude Code 여부, 그 외 도구)
- 작업 방식/워크플로 (기획 문서 → 구현 → 검증 순서, 사람이 개입한 지점)
- 대표 프롬프트·세션 사례 2~3개 — 예를 들어:
  · Slay the Spire 스타일 방 그래프(silverua MIT 구현) 이식 과정에서의 AI 활용
  · 방 이벤트/증강/군대 배치 UI 등 주요 시스템 구현 지시 방법
  · EditMode 테스트 271개를 어떤 방식으로 작성·유지했는지
- 산출물 검증 방법 (테스트, 플레이 확인 등)
-->

- _(담당 팀원 작성 예정)_

## 6. VARCO 활용 — 병사 3D 캐릭터 제작

병사 3D 모델 5종(기본 병사·전사·궁수·사냥꾼·암살자, `Assets/OutGame/Arts/ClayUnit/`)은 VARCO를 이용해 제작했습니다.

<!-- [담당 팀원 작성 예정]
권장 포함 내용:
- 사용한 VARCO 제품/기능 명칭과 버전
- 입력 프롬프트 예시 (병사 모델을 어떤 프롬프트로 생성했는지 1~2개)
- 생성 후 후처리 과정 (리깅/텍스처/스케일 조정 등 Unity 임포트까지의 작업)
- 반복 횟수·선별 기준 (몇 번 생성해서 어떤 기준으로 골랐는지)
-->

- _(담당 팀원 작성 예정)_

## 7. 이미지 제작 — ChatGPT · Gemini

게임 내 2D 이미지는 ChatGPT와 Gemini를 이용해 제작했습니다.

<!-- [작성 예정]
권장 포함 내용:
- 제작한 이미지 목록 (예: UI 아이콘, 이벤트 방 일러스트 등 — 실제 사용처 기준)
- 도구별 분담 (어떤 이미지를 ChatGPT로, 어떤 이미지를 Gemini로 만들었는지)
- 입력 프롬프트 예시 1~2개
- 생성 후 후처리 (배경 제거, 리사이즈, 톤 보정 등)
-->

- _(작성 예정)_

## 8. 초기 단계 (프로토타입 0~4단계) AI 활용

<!-- [작성 예정]
5단계 이전 — 프로젝트 셋업, 전투 시뮬레이션 최초 구현, 스트레스 테스트,
로직/뷰 분리 아키텍처 확립 등 — 의 AI 활용 내역.
세션 로그가 없는 구간이므로 git 커밋 이력을 근거로 소급 정리.
-->

- _(작성 예정)_

## 9. 산출물 검증 방법

AI 산출물은 다음 다층 검증을 통과해야 완료로 인정했습니다. 핵심은 **AI의 완료 주장이 아니라 재현 가능한 증거로 완료를 판정한다**는 것입니다.

| 층 | 수단 | 최종 상태 |
|---|---|---|
| 컴파일 | MCP for Unity로 컴파일 에러 확인 | 에러 0 |
| 단위/통합 테스트 | Unity EditMode 테스트 (인게임 + 아웃게임) | 305/305 통과 |
| 밸런스 회귀 | BalanceLab 기준 시드 전투 결과 보존 확인 | 유지 (시드 0 = 339틱) |
| 통계 검증 | 시나리오 매트릭스 (14종 × 200판) 승률 밴드 판정 | 파이프라인 가동 |
| 정본 등가성 | 에디터↔CLI 교차 검증 (시드 100개, 승자 무관용) | 뒤집힘 0건 |
| 플레이 확인 | 실제 씬 구동 (전투 진행·결과 표시·런타임 에러) | 정상 |

- 아키텍처 원칙("콘텐츠 추가는 데이터만으로") 자체도 테스트로 강제해, 원칙을 어기는 코드가 들어오면 즉시 실패하게 했습니다.
- 검증 장치 자체도 검증했습니다: 수치 불일치 가드에 일부러 어긋난 값을 넣어 정확히 잡아내는지(종료 코드 1 반환) 확인하는 식으로, 도구의 신뢰성을 실측으로 증명하고 로그에 남겼습니다.

## 10. 외부 에셋 / 오픈소스 출처

AI 생성물 외에 사용한 외부 리소스는 다음과 같습니다.

| 리소스 | 용도 | 출처 / 라이선스 |
|---|---|---|
| Medieval Castle (Advance Studios) | 전장 배경 (성·성벽) | Unity Asset Store <!-- [링크 추가 예정] --> |
| Magic Effects Pack (Hovl Studio) | 스킬·전투 이펙트 | Unity Asset Store <!-- [링크 추가 예정] --> |
| Hand Painted Seamless ClayGround Texture | 지형 텍스처 | <!-- [출처 추가 예정] --> |
| 2D Simple UI Pack (OArielG) | 아웃게임 UI | <!-- [출처 추가 예정] --> |
| DNFBitBitv2 | 폰트 | 네오플 무료 폰트 <!-- [라이선스 표기 확인] --> |
| silverua — Slay the Spire 스타일 맵 구현 | 방 그래프 생성 알고리즘 이식 | GitHub, MIT License |
| DOTween (Demigiant) | 트위닝 라이브러리 | 무료 버전 |
| 효과음 (Sounds/Units) | 병과별 공격 사운드 | <!-- [담당 팀원 확인 후 추가 예정] --> |

- 병사 3D 모델(ClayUnit 5종)은 외부 에셋이 아니라 VARCO로 자체 제작한 AI 생성물입니다 (§6).
- 게임 내 2D 이미지 일부는 ChatGPT·Gemini로 자체 제작한 AI 생성물입니다 (§7).
