# BalanceLab — 밸런싱 파이프라인 (CLI 러너 + 웹 뷰어)

## 사용법

```
cd Tools/BalanceLab
dotnet run -- scenarios/all.json          # 전체 매트릭스 (14개 시나리오) + PASS/FAIL 요약
dotnet run -- scenarios/<시나리오>.json    # 단일 시나리오
```

| 옵션 | 뜻 |
|---|---|
| `--tag <라벨>` | 결과를 `<시나리오>.<라벨>.json`으로 저장 — 정본 스냅샷을 덮어쓰지 않는다 |
| `--overrides <파일>` | `.asset` 수치를 덮어써 실험한다 (**`--tag` 필수**) |
| `--replay <시드,...>` | 해당 시드의 궤적을 덤프해 뷰어에서 재생 가능하게 한다 |

종료 코드: `0` 전부 PASS / `1` 오류 / `2` 사용법 오류 / `3` 밴드 FAIL 존재 (튜닝 루프의 종료 조건).
모르는 옵션은 즉시 거절한다 — 오타 난 플래그가 조용히 무시되면 "옵션을 줬는데 아무 일도 안 일어나는" 상황이 된다.

## 뷰어

`viewer/index.html`을 **더블클릭**하면 열린다. 서버가 필요 없다 — CLI가 결과를 `.json`과 함께 `.js`
래퍼로도 쓰기 때문에 `file://`에서도 `<script>`로 로드된다 (`fetch`는 CORS로 막힌다).

세 화면:

1. **매트릭스** — 14개 시나리오의 좌군 승률을 목표 밴드 띠 위에 세로선으로 겹쳐 그린다. "이탈만 보기" 필터가 작업 큐.
2. **시나리오 상세** — 종료 틱 분포, 시드별 승자 그리드, 분대 요약. **시드 직접 입력** 지원:
   덤프된 시드면 재생 화면으로, 범위 안이지만 미덤프면 기록을 펼치고 덤프 명령을 주고, 범위 밖이면 범위를 알린다.
3. **리플레이 + 튜닝** — 탑다운 2D 재생(재생/스크럽/배속/프레임 스텝, 스페이스바 토글)과 슬라이더 패널.
   슬라이더는 `.asset` 원본값(`results/assets.js`)을 기준선으로 잡고 **델타만 축적**하므로,
   내보내는 `overrides.json`은 항상 최소 패치다. 내보내기는 실행 명령까지 함께 준다.

`--tag` 결과가 있으면 매트릭스에서 **정본 대비 델타**(`80.0% → 61.0% ▼19.0p`)를 볼 수 있다.

### 튜닝 루프

```
뷰어에서 슬라이더 조절 → overrides.json 내보내기 → tuning/에 저장
  → dotnet run -- <시나리오> --overrides tuning/<이름>.json --tag <이름>
  → 뷰어 새로고침 → 비교 모드로 정본과 대조
  → 확정되면 Unity에서 .asset에 손으로 반영 → 태그 없이 재실행 → 정본 갱신
```

정본 승격에 사람의 손이 한 번 들어가는 것이 안전장치다. 뷰어는 `.asset`을 쓰지 않는다.

### 출력 파일

| 파일 | 소비자 |
|---|---|
| `results/<시나리오>[.<태그>].json` | 사람, 교차 검증 테스트 (정본) |
| `results/<시나리오>[.<태그>].js` | 뷰어 (같은 내용의 `<script>` 래퍼) |
| `results/index.json` / `index.js` | 매트릭스 진입점. `(name, tag)` 쌍으로 누적 갱신되고, 결과 파일이 사라진 항목은 자동으로 빠진다 |
| `results/assets.js` | 뷰어 튜닝 패널의 기준선 (오버라이드 **적용 전** `.asset` 수치) |
| `results/replay/<시나리오>[.<태그>].<시드>.js` | 리플레이 뷰어 |

## 시나리오와 목표 밴드

- `scenarios/all.json` — 세트 파일(`scenarios` 배열). 전체 매트릭스를 한 번에 돌린다.
- `scenarios/matrix/*.json` — 장군 유/무 4종 + 미러 4종 + 롤 상성 6쌍 = 14개.
- `armies/*.json` — 군대 정의 재사용 파일. 시나리오에서 `"left": "@../../armies/Warrior_general.json"`으로 참조한다.
- `bands.json` — 목표 밴드. 시나리오의 `band` 태그로 규칙을 고르고 **좌군 승률**로 판정한다.
  미등록 태그는 즉시 실패한다(오타가 조용히 "판정 없음"이 되지 않도록).

| 밴드 | 목표 | 의미 |
|---|---|---|
| `general_vs_none` | 65~75% | 장군의 가치는 분명하되 절대적이지 않아야 한다 |
| `mirror_general` | 45~55% | 동일 구성 매치 — 좌/우 진영의 구조적 유불리가 없어야 한다 |
| `role_matchup` | 30~70% | 롤 상성은 있되 일방적이지 않아야 한다 |
| `none` | 판정 없음 | 관찰·회귀 확인 전용 |

- 시드 0..N-1 고정 실행 → `results/<시나리오명>.json` 생성 (판별 기록 포함 — 모든 시드는 언제나 재현 가능)
- guid→경로 인덱스는 캐싱 없이 매 실행 재구축된다.
- 시나리오 JSON에는 스탯을 넣지 않는다 (수치 출처는 `.asset`).

## 스탯 출처

| 무엇 | 어디서 |
|---|---|
| 인게임 속성 (공격 주기·사거리·투사체·타겟팅·이동 패턴·반경) | `.asset` (SquadData) |
| 장군 능력 (패시브·충전·액티브·리드 오프셋) | `.asset` (SquadData의 장군 필드, 2026-08-10 병사/장군 통합) |
| 로컬 기준 5스탯 (체력·공격력·방어력·치명타·이동속도) | `.asset` — 적 구성·로컬 시나리오의 기준선 |
| **실전 플레이어 부대 스탯** | 아웃게임 (`ArmyData` 원형 × `ArmyStatCalculator` 배율) — 커넥터가 결합 |

**튜닝 절차**: `.asset` 수정 → 재실행. 인게임 속성(공격 주기·사거리·타겟팅)과 장군 능력은 여기서 튜닝하는 것이 맞다.
다만 **플레이어 부대의 5스탯은 아웃게임이 정본**이므로, 그 수치를 바꿀 일이면 아웃게임 `ArmyDefinition`을 고쳐야 한다.

**레벨 스윕은 현재 없다** — 업그레이드 배율(0~5)의 공식은 `ArmyStatCalculator`가 단일 출처이고, 그 공식을
툴에서 재현하면 양쪽 수치가 갈라진다. dev 머지 후 `OutGame.Logic` 소스를 csproj에 링크해 되살린다
(Simulation을 링크한 것과 같은 방식).

## 정본 분리 원칙 (교차 검증 계약)

| 역할 | 정본 |
|---|---|
| 밸런싱 통계 (승률·분포·리포트) | **CLI (.NET)** |
| 시드 리플레이 (눈 검증) | **CLI (.NET)** — 통계와 같은 실행에서 나온 궤적 |
| 손맛·연출 확인 | 에디터 테스트 씬 (`BattleTestBootstrap`) — 정본 아님 |

**리플레이 정본이 에디터에서 CLI로 바뀌었다** (뷰어 도입, `Tools/BalanceLab/밸런스 뷰어 설계.md` §2).
이유: 대시보드에서 "좌군 승률 80%, 진 판 중 시드 17을 보자"를 눌렀을 때 보여줄 판은 **그 승률을 만든
바로 그 판**이어야 한다. 에디터가 같은 시드를 재시뮬하면 아래의 드리프트 때문에 다른 전투가 나올 수 있고,
통계와 화면이 어긋나는 순간 도구로서의 의미가 사라진다.

`BalanceLabCrossCheckTests`는 그대로 유지한다. 역할이 오히려 명확해졌다 — *게임에 실제로 들어가는
Mono 런타임이 CLI 정본과 여전히 통계적으로 같은가*를 감시하는 회귀 가드다.

두 런타임은 JIT float 정밀도 차이(Mono가 확장 정밀도 중간값을 허용 — ECMA CIL 표준 동작)로
**비트 단위 일치가 불가**함이 실측으로 확인됐다 (FMA·하드웨어 인트린식 비활성화 실험으로 소거 —
상세 경위는 Assets/Docs/ai_usage_log.md). 따라서 교차 검증(`BalanceLabCrossCheckTests`)은
결정론이 아니라 **두 정본의 통계적 등가성**을 감시한다:

- **승자 불일치 = 즉시 FAIL (무관용)**
- 종료 틱 **상대 오차 ±20% 허용** (절대 틱이 아닌 이유: 드리프트는 나비효과라 전투 길이에 비례해 커진다 —
  실측 최대 15.3%, 일제 사격 시나리오. 여유를 둬 ±20%로 확정)
- 검증 시드 **100개** — "100시드 승자 뒤집힘 0건"이 이 계약의 신뢰 근거
- 리플레이가 CLI 정본이 된 뒤에도 이 계약은 그대로다 — 감시 대상이 "리플레이의 정확성"에서
  "게임 런타임(Mono)이 정본과 여전히 같은가"로 바뀌었을 뿐이다
- 작업 2에서 미러 매치업(45~55% 밴드) 시나리오가 생기면 교차 검증 대상에 추가한다 —
  박빙 판이야말로 승자 안정성의 진짜 스트레스 테스트다
- 교차 검증 테스트는 시나리오에 레벨이 남아 있으면 실패시킨다 — 레벨은 CLI 전용 대역이라 에디터 경로가
  재현할 수 없기 때문. (현재 레벨 기능 자체가 없으므로 이 가드는 머지 후 되살릴 때를 위한 안전장치다.)

### 실측 (2026-07-24, 시드 100개 × 2종)

| 시나리오 | 승자 뒤집힘 | 최대 틱 드리프트 | 판정 |
|---|---|---|---|
| warrior_general_vs_plain (방진, 삼각함수 미사용) | 0건 | 18틱 / 352틱 = 5.1% | PASS |
| archer_general_volley_cross (일제 사격, Sin/Cos 경로) | 0건 | 50틱 / 327틱 = 15.3% | PASS |

완전한 크로스 런타임 결정론(고정소수점 수학 전환)은 **잼 이후 과제**로 보류한다.

## 교차 검증 실행 절차

1. CLI로 두 시나리오 실행 (results/ 생성):
   `dotnet run -- scenarios/warrior_general_vs_plain.json`
   `dotnet run -- scenarios/archer_general_volley_cross.json`
2. Unity EditMode 테스트 실행 → `BalanceLabCrossCheckTests`가 시드 0..99를 에디터 인프로세스로
   재실행해 CLI 기록과 비교한다. (결과 파일이 없으면 Inconclusive)
